---
description: 测试规范（测试文件触发）
globs:
  - "**/*Test.java"
  - "**/*Tests.java"
  - "**/*IT.java"
  - "**/__tests__/**"
  - "**/*.spec.js"
  - "**/*.test.js"
  - "**/*.spec.jsx"
  - "**/*.test.jsx"
  - "**/*.spec.ts"
  - "**/*.test.ts"
  - "**/*.spec.tsx"
  - "**/*.test.tsx"
  - "**/*.cy.ts"
  - "**/*.cy.tsx"
  - "**/*.e2e.ts"
  - "**/*.e2e.tsx"
---

# 50 测试规范

## 适用范围

- 适用于：`**/*Test.java`、`**/*Tests.java`、`**/*IT.java`、`**/__tests__/**`、`**/*.spec.{js,ts,jsx,tsx}`、`**/*.test.{js,ts,jsx,tsx}`、`**/*.cy.{ts,tsx}`、`**/*.e2e.{ts,tsx}`、测试相关文件
- 不适用于：CI/CD 编排、发布审批、部署策略（见 `58-cicd.rules.md`）

## 总体策略

1. **分层测试**：按目标拆分单元、组件、集成、契约、端到端测试，避免所有验证堆在同一层。
2. **快速反馈优先**：默认本地和 PR 快速链路只跑可自举、可重复、定位清晰的测试。
3. **风险驱动自动化**：安全、权限、多租户、数据库迁移、插件边界等高风险变更优先自动化回归。

---

## 禁止（Must Not）

### 1) 分层与门禁

- ❌ 用一次手工验证替代应自动化回归的 bug 修复、核心业务逻辑或高风险变更。
- ❌ 将单元测试、集成测试、E2E 混成同一套大而慢且难定位问题的测试集合。
- ❌ 将依赖真实 MySQL / Redis / MQ / 外部网络的测试直接混入默认 `mvn test`、`npm run test`、`npm run test:unit` 等快速反馈链路。
- ❌ 因测试不稳定而长期保留 `@Disabled`、`it.skip`、`test.skip`、`test.only`、`describe.only`。

### 2) 数据与稳定性

- ❌ 使用生产数据或共享环境中的脏数据作为测试输入。
- ❌ 测试依赖当前时间、随机数、系统时区、系统语言、端口占用、共享外部状态而不做控制。
- ❌ 测试用例之间相互依赖（执行顺序影响结果）。
- ❌ 异步测试没有超时上限、结束条件或重试边界。

### 3) 实现质量

- ❌ 只有“跑通”没有断言的测试。
- ❌ 测试代码承载复杂业务逻辑，导致测试本身难以审查和维护。
- ❌ 前端测试只保留快照而不验证关键交互、权限状态、表单校验、请求行为。
- ❌ 将 MySQL 方言兼容性、Liquibase/Flyway 迁移、索引/唯一约束行为只交给 H2、mock 或纯内存替代验证。
- ❌ 通过 `wrapper.vm`、`vm.$emit(...)`、直接调用私有方法、强行改内部状态等方式去覆盖真实用户在 UI 中不可达的分支；除非第三方组件限制导致无法用真实交互表达，并且测试中必须明确说明原因。
- ❌ 为了抬高 coverage 新增纯 getter/setter、纯 POJO、纯 Lombok/record 访问器测试；这类测试如果不验证校验约束、默认值、序列化、生命周期回调或兼容性契约，就不应添加。
- ❌ 端到端或组件测试绕过真实的禁用态、确认弹层、权限门禁、租户门禁，导致“测试可操作但用户不可操作”。

### 4) E2E 设计与真实链路

- ❌ 将 UI 组件测试、接口集成测试、真实链路 E2E 混称为同一种“E2E”，导致环境、门禁、失败定位和维护责任不清。
- ❌ 在目标是验证真实前后端联动、真实认证链路、真实租户上下文、真实数据库语义时，继续用 `page.route()`、伪造 JWT、伪造 `localStorage/sessionStorage`、手工塞 `storageState` 来替代真实链路。
- ❌ E2E 没有明确说明自己属于哪一类：mock-assisted UI、isolated real-link、shared-env smoke、nightly/full-chain。
- ❌ E2E 依赖不可重置的共享脏数据、人工手工前置步骤、个人账号状态或不受控的外部环境漂移。
- ❌ 用“接口返回 200”代替对用户可观察结果的断言，如页面状态、列表变化、错误提示、最终状态收敛、审计记录或下载结果。
- ❌ 在真实链路 E2E 中只验证 happy path，不覆盖关键拒绝路径、失败路径、取消路径、幂等或重复点击路径。

---

## 必须（Must）

### 1) 测试分层

- ✅ 单元测试：领域逻辑、工具类、Service 纯逻辑、前端 composable、Pinia store、表单校验函数必须优先使用单元测试验证。
- ✅ 组件测试：Vue 组件、复杂表单、权限驱动 UI、交互状态切换必须使用组件测试或前端单测覆盖关键行为。
- ✅ 集成测试：跨模块协作、数据库访问、控制器到持久层链路、Spring 配置装配等改动必须提供集成测试。
- ✅ 契约测试：API 响应结构、插件扩展点、消息体、第三方集成协议发生变化时，必须提供兼容性验证。
- ✅ 冒烟 / E2E：影响关键用户路径的改动必须补充冒烟或端到端验证，至少覆盖登录、权限校验、租户上下文、关键提交链路中的相关场景。

### 2) 变更与测试责任

- ✅ 修复 bug / 新增逻辑必须提供自动化测试；仅文档、纯文案、纯样式微调等低风险改动才可只写明确验证步骤。
- ✅ 涉及数据库查询、事务、MySQL 方言、Liquibase/Flyway 迁移、索引/约束行为的改动，必须提供真实数据库验证方案。
- ✅ 涉及认证、权限、租户隔离、幂等性、插件边界的改动，必须至少覆盖“允许路径 + 拒绝路径 + 回归场景”。
- ✅ 测试类型必须与风险匹配：纯逻辑问题补单元测试，跨边界问题补集成测试，关键用户路径补冒烟/E2E。
- ✅ 每个缺陷关闭前都必须有可重复执行的回归验证，优先自动化而不是依赖口头确认。

### 3) 测试用例设计

- ✅ 测试命名必须清晰表达场景与预期，推荐格式：`方法或行为_场景_预期结果`。
- ✅ 测试结构必须遵循 AAA 或 Given-When-Then；每个测试方法只验证一个主要行为。
- ✅ 测试断言必须显式验证结果、异常、状态变化或副作用，不能只验证“无报错”。
- ✅ 用例至少覆盖正常路径、边界条件、异常路径；高风险逻辑还必须覆盖权限、租户、幂等、并发或重复提交等关键约束。
- ✅ 前端测试除断言渲染结果外，还必须验证事件触发、禁用态、加载态、错误提示、路由跳转或请求参数等关键行为。
- ✅ 组件测试必须优先通过用户真实可触发的路径驱动：点击、输入、选择、确认、路由跳转、请求回调；如果某个按钮在真实界面中是 disabled，测试应断言禁用原因，而不是手动触发其事件。
- ✅ stub / mock 必须保留关键交互语义，至少包括：`disabled`、`loading`、`v-model`、`confirm`、路由跳转、错误提示；不能把关键门禁语义在测试替身里抹掉。
- ✅ 为提升覆盖率新增的 DTO / model / exception 测试，必须至少验证以下之一：校验注解、默认值、格式化/序列化约定、实体生命周期回调、异常 errorCode 映射；不能只覆盖访问器。
- ✅ 对编排型业务（如调度 / 工作流 / 统计 DAG / 任务编排）必须覆盖拓扑语义，而不是只覆盖接口名：至少验证一条并行分叉后归并、一条串行推进，以及一条失败后重试 / 取消 / 暂停恢复路径，并断言节点释放顺序与最终状态。

### 4) 测试数据与环境

- ✅ 测试必须固定时钟、随机种子、时区、语言环境或显式隔离相关影响因素。
- ✅ 测试数据必须通过 Builder、Factory、fixture、`@Sql`、测试脚本等方式显式构造，并在测试后清理。
- ✅ 默认快速测试链路必须可在干净工作区自举运行，不依赖开发者本地手工准备的数据库状态。
- ✅ 慢测试、外部依赖测试、真实基础设施测试必须通过 tag、profile、独立任务或独立 CI job 隔离。
- ✅ 数据库行为确实依赖 MySQL 方言时，必须使用真实 MySQL、Testcontainers 或 CI 提供的等价服务验证；H2 仅可用于方言无关场景。

### 5) 测试身份与账号

- ✅ 集成测试、冒烟测试、E2E、需要经过真实认证链路的自动化测试，必须使用专用自动化账号、专用测试租户、专用 OAuth 客户端或等价测试身份。
- ✅ 自动化测试身份必须与测试目标匹配，至少覆盖：普通用户、租户管理员、无权限或拒绝用户、跨租户拒绝用户；启用 MFA 的场景还必须覆盖 MFA 用户。
- ✅ 测试账号、测试密码、TOTP secret、client secret、测试租户 ID 等敏感值必须来自测试环境种子数据、CI secret 或受控配置，不能硬编码在测试代码和仓库明文文件中。
- ✅ 自动化测试身份必须可初始化、可重置、可重复使用；测试结束后不应留下污染后续执行的状态。
- ✅ 禁止使用开发者个人账号、共享人工管理员账号、生产账号或生产租户进行自动化测试。

### 6) E2E 设计要求

- ✅ 每条 E2E 必须明确所属层级，至少标明以下之一：`mock-assisted UI`、`isolated real-link`、`shared-env smoke`、`nightly/full-chain`。
- ✅ E2E 必须在用例文件、README、测试命令或配置中说明以下信息：环境来源、身份来源、数据 seed/reset 方式、依赖的后端/数据库/认证链路、允许的 mock 边界。
- ✅ 真实链路 E2E 必须优先验证用户最终可观察结果：页面状态变化、权限拒绝、租户隔离、提交成功后的列表/详情变化、任务最终状态、审计/历史记录，而不是只断言网络请求发出。
- ✅ 当目标是验证真实前后端联动、真实认证、真实租户、真实数据库语义时，必须走真实链路；此时对 first-party 业务 API 不得再用 `page.route()` 替代。
- ✅ E2E 所需 seed/reset 必须幂等、可重复执行；测试运行前后的环境状态必须可预测。
- ✅ 涉及长流程、异步编排、状态机收敛的 E2E，必须断言中间关键状态与最终状态，而不是只等待最后一个 toast。
- ✅ E2E 必须对关键高风险路径补拒绝场景或失败场景，至少覆盖权限拒绝、租户拒绝、重复提交、取消/回滚/恢复中的相关一类。
- ✅ 如果使用 `storageState`、cookie 预置、session 预置，它们必须来源于真实登录 setup，而不是手写伪造状态；当测试目标本身是登录/MFA/OIDC 时，必须直接覆盖真实登录步骤。

### 7) 前端测试要求

- ✅ 前端业务逻辑必须尽量下沉到 composable、store、service 或纯函数中，保证可测试性。
- ✅ 修改 composable、Pinia store、路由守卫、权限判断、复杂表单状态机、请求适配层时，必须补前端自动化测试。
- ✅ 关键页面交互不能只依赖 `vue-tsc`、ESLint 或人工点点看；类型正确不等于行为正确。
- ✅ 前端异步测试必须控制定时器、网络请求和异步刷新机制，避免依赖真实后端或偶发时序。
- ✅ 前端组件测试应优先断言“用户看到什么、能点什么、点了以后会发生什么”，而不是断言组件私有实现细节。
- ✅ 使用 Ant Design Vue、Element Plus 等组件库时，测试替身必须保留组件的关键禁用/确认/表单提交语义；如果替身过度简化导致路径失真，应改用更真实的 stub 或补 E2E。

### 8) 测试审查清单

- ✅ 新增测试是否覆盖真实可达路径，而不是测试专用捷径。
- ✅ 新增测试是否验证了业务约束、权限、租户、状态机、拓扑语义或兼容性契约，而不是只验证访问器。
- ✅ 新增测试失败时是否能清楚说明哪条业务语义被破坏。
- ✅ coverage 提升是否来自高价值路径，而不是低价值机械覆盖。
- ✅ E2E 是否写清了环境类别、seed/reset、身份来源和允许的 mock 边界。
- ✅ 真实链路 E2E 是否避免把 first-party API mock 掉之后仍宣称“端到端验证通过”。

---

## 应该（Should）

### 1) 覆盖范围

- ⚠️ 关键业务逻辑、Controller、Repository、前端 composable / store / 权限控制代码应保持高覆盖，建议核心路径覆盖率达到 80% 以上。
- ⚠️ 覆盖率只作为辅助信号，不应替代测试分层和关键场景验证。
- ⚠️ 对 bug 修复应优先补“先失败后通过”的回归测试。

### 2) 组织方式

- ⚠️ 单元测试应明显多于集成测试，集成测试应明显多于 E2E，保持测试金字塔。
- ⚠️ 使用参数化测试、表驱动测试覆盖多输入场景，避免复制粘贴大量相似测试。
- ⚠️ 使用测试数据工厂、测试 DSL 或公共 fixture 减少样板代码。
- ⚠️ E2E 应按“最快冒烟 -> 模块关键链路 -> 真实长链路”分层，不要把所有场景都堆到最慢的 full-chain 套件里。

### 3) 前端专项

- ⚠️ Vue 组件测试优先覆盖用户可观察行为，而不是组件私有实现细节。
- ⚠️ 表单、上传、权限控制、租户切换、长列表筛选等关键页面建议提供组件测试或 E2E 测试。
- ⚠️ 前后端接口变化频繁的模块建议补充契约测试或 Mock Server 校验，降低联调回归成本。
- ⚠️ 对复杂组件优先抽出可单测的纯逻辑，再用组件测试覆盖入口和关键交互，避免为了覆盖 UI 分支去绕过真实用户路径。
- ⚠️ 前端测试中的 stub 应尽量最小但语义真实；一旦 stub 让测试可以执行用户实际做不到的动作，应视为无效测试信号。

### 4) 测试身份治理

- ⚠️ 自动化测试身份建议使用稳定命名和固定职责，如 `e2e-user`、`e2e-tenant-admin`、`e2e-mfa-user`、`e2e-cross-tenant-deny`。
- ⚠️ 测试身份矩阵建议集中维护在种子脚本、fixture 或文档中，避免各测试文件各自散落创建。
- ⚠️ 启用 MFA、OIDC、Session / JWT 切换的模块，建议提供一键初始化测试身份和测试租户的脚本。

### 5) 数据库与外部依赖

- ⚠️ Repository/JPA/MyBatis 查询正确性测试，优先使用与生产方言接近的验证方式，而不是默认依赖 H2 代理。
- ⚠️ 外部依赖测试应具备确定性：固定 profile、固定建库脚本、固定初始化方式、固定镜像版本。
- ⚠️ 长耗时测试应独立标记，避免拖慢默认开发反馈链路。

### 6) E2E 专项

- ⚠️ 真实链路 E2E 建议把鉴权 setup、seed/reset、运行用例拆开，避免把环境准备耦合进每条用例。
- ⚠️ 模块级 E2E 应优先覆盖“一个完整业务闭环”而不是大量互相重叠的 happy path。
- ⚠️ 共享环境 smoke 建议只覆盖只读或幂等路径；有副作用的写操作优先放在可重置的隔离环境中。
- ⚠️ 对认证、租户、工作流、调度、插件装载等跨边界场景，建议至少保留一条 real-link E2E，而不是只靠组件测试或 API mock。

---

## 可以（May）

- 💡 视觉回归测试：对高价值页面增加截图比对，但不能替代行为断言。
- 💡 契约测试：使用 Pact、OpenAPI diff、Schema 校验等方式保证接口兼容性。
- 💡 性能与容量 smoke：对热点接口、关键前端渲染路径增加轻量性能基线测试。
- 💡 基于属性的测试：对复杂规则引擎、表达式解析、校验器可使用 property-based testing。

---

## 例外与裁决

### 文档与低风险改动

- 文档、注释、纯文案、纯样式微调可不强制新增自动化测试，但必须提供明确验证步骤。

### 遗留模块

- 遗留代码允许渐进治理，但只要本次改动触达关键逻辑，就应补充当前改动范围内的最小回归测试。
- 已复现的线上缺陷在关闭前必须补回归测试，不能只依赖手工复测。

### 紧急修复

- 紧急修复可先走最小验证链路，但必须在合并后立即补齐自动化回归，并记录风险与回滚方案。

### 冲突裁决

- 平台特定规则（90+）优先于本规范。
- CI/CD 执行链路与质量门禁以 `58-cicd.rules.md` 为准。
- 测试规范与业务规范冲突时，优先保证安全、权限、多租户隔离不被弱化。

---

## 示例

### ✅ 正例：Java 回归测试（覆盖成功与失败路径）

```java
@Test
void createUser_whenTenantMismatch_throwsForbidden() {
    // Arrange
    Long requestTenantId = 100L;
    Long currentTenantId = 200L;
    CreateUserCommand command = new CreateUserCommand(requestTenantId, "alice");
    when(tenantContext.getCurrentTenantId()).thenReturn(currentTenantId);

    // Act + Assert
    assertThatThrownBy(() -> userService.createUser(command))
        .isInstanceOf(ForbiddenException.class)
        .hasMessageContaining("tenant");
}
```

### ✅ 正例：Vue 组件测试（断言用户可观察行为）

```ts
it('submits form after validation passes', async () => {
  const submit = vi.fn().mockResolvedValue(undefined)
  const wrapper = mount(UserForm, {
    props: { onSubmit: submit }
  })

  await wrapper.get('[data-test=username]').setValue('alice')
  await wrapper.get('[data-test=submit]').trigger('click')

  expect(submit).toHaveBeenCalledWith(
    expect.objectContaining({ username: 'alice' })
  )
  expect(wrapper.find('[data-test=error]').exists()).toBe(false)
})
```

### ✅ 正例：自动化测试身份矩阵

```text
- e2e-user: 普通用户，仅能访问当前租户普通资源
- e2e-tenant-admin: 当前租户管理员
- e2e-cross-tenant-deny: 登录成功但访问其他租户资源应被拒绝
- e2e-mfa-user: 必须经过 TOTP 流程
```

### ❌ 反例：只有执行没有断言，且依赖真实环境

```java
@Test
void testCreateOrder() {
    orderService.createOrder(command); // ❌ 没有断言
    // ❌ 依赖开发者本地数据库已有数据
}
```

