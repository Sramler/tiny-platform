---
description: CI/CD 规范（质量门禁与发布流程，始终应用）
alwaysApply: true
---

# 58 CI/CD 规范

## 适用范围

- 适用于：全仓库的构建、测试、质量门禁、发布、部署、回滚流程
- 不适用于：仅个人本地、不会进入共享分支或发布链路的一次性实验

## 总体策略

1. **流水线即规则**：能自动检查的质量要求必须沉淀到流水线，而不是依赖口头约定。
2. **分级门禁**：本地、PR、主干、发布使用不同层级的门禁，兼顾反馈速度和风险控制。
3. **同一制品逐级晋升**：发布应复用已验证产物，而不是在不同环境重复构建不同制品。

---

## 禁止（Must Not）

### 1) 合并与放行

- ❌ 在必需检查失败、缺失或被跳过的情况下合并代码。
- ❌ 通过反复重试、临时关闭规则、改动流水线阈值来掩盖真实失败。
- ❌ 绕过受保护分支、审查要求或质量门禁直接发布生产变更。

### 2) 构建与制品

- ❌ 流水线依赖开发者本地未提交文件、手工拷贝产物或临时机器状态。
- ❌ 同一提交在不同环境重新构建出不同版本的制品后再比较“哪个能跑”。
- ❌ 将 SNAPSHOT、未锁定依赖或来源不明制品用于正式发布链路。

### 3) 部署与回滚

- ❌ 无回滚方案、无健康检查、无发布后验证就直接部署生产环境。
- ❌ 绕过版本化迁移工具直接手工改生产数据库结构。
- ❌ 影响安全、权限、多租户边界的变更在未追加专项验证的情况下进入生产。

---

## 必须（Must）

### 1) 本地开发门禁

- ✅ 仓库必须提供清晰、可执行的本地验证命令，至少覆盖构建、格式检查、静态检查、快速测试。
- ✅ 开发者提交前必须运行与改动范围匹配的快速验证链路；默认应包括 format / lint / typecheck / unit 中的相关项。
- ✅ 本地快速链路必须可在干净工作区运行，不依赖手工准备的外部基础设施或个人环境状态。
- ✅ 慢测试、真实数据库测试、E2E、性能测试必须与本地快速链路隔离。
- ✅ 如果仓库已提供“本地自举 / 自愈”验证脚本，本地与 AI 助手应优先复用它们，而不是各自发明一套启动顺序；对 tiny-platform 而言，**默认入口**是 `verify-platform-local-dev-stack.sh`，仅在明确不需要前端联动时才降级到 `verify-platform-dev-bootstrap.sh`，仅在纯 Maven 门禁时才使用 `mvn-tiny-oauth-server-gate-sequential.sh`。
- ✅ 同一 Maven 模块的 `compile` / `test` / `package` 在本地门禁中必须顺序执行；共享 `target/` 的并发执行属于执行噪声，不能作为代码回归依据。
- ✅ 本地验证脚本如果区分 `exit 0/1/2`，流水线和报告必须保留该语义：`exit 2` 代表环境前置未满足，不得记为代码失败。

### 2) PR 门禁

- ✅ PR 必须通过编译或构建校验、格式检查、静态检查、单元测试、前端类型检查，以及与改动范围匹配的集成测试。
- ✅ `sb3` 与 `sb4` 可以有不同的 required checks，但两条受保护分支都必须显式配置门禁；不得依赖默认继承、临时人工判断或口头约定。
- ✅ 当前 CI/CD 口径必须与分支治理一致：`sb4` 是默认业务主干，`sb3` 是维护线；`sb3` backport 进入 PR 前必须在 `sb3` 目标语义下重新执行相关门禁，不能直接复用 `sb4` 的通过结果。
- ✅ 涉及数据库变更时，PR 必须包含迁移脚本校验或迁移 smoke 验证。
- ✅ 涉及认证、权限、多租户、配置、安全边界的改动时，PR 必须追加专项自动化验证，且人工审查不可省略。
- ✅ 需要真实认证链路的自动化测试必须使用专用测试账号、测试租户、测试 client，并从 CI secret 或受控测试配置注入。
- ✅ 流水线在执行依赖测试身份的 job 前，必须校验所需凭证和测试身份已准备就绪；缺失时应快速失败并给出明确信息。
- ✅ PR 快速门禁中的 E2E 必须限制在可快速自举、可重复、可定位的 smoke 或 mock-assisted / isolated real-link 子集，不得把全量 full-chain 场景直接塞进默认 PR 必跑链路。
- ✅ PR 涉及关键用户路径且存在稳定 E2E 时，必须至少运行与改动范围匹配的一条 smoke 或模块级 real-link E2E；若因环境限制无法执行，必须在 PR 中写明缺口与补跑计划。
- ✅ PR 若修改 Vue Router guard、动态菜单路由注入、菜单驱动页面入口、壳页跳转或第三方组件运行时契约（如 `a-table` 树表展开 / 懒加载），必须追加针对该链路的自动化回归：至少锁住 direct deep-link / 刷新路径或真实 prop / event 契约之一；不能只靠“手工点菜单能进”放行。
- ✅ PR 若修改内部 fork 依赖坐标、包版本、发布仓库、workflow fallback 或 consumer 引入方式，必须至少验证一条 consumer 路径（编译、启动或门禁 workflow）；不能只证明“fork 仓库本身能 build”。
- ✅ PR 失败必须阻断合并；除紧急例外外，不允许带红合并。
- ✅ PR 描述必须写清验证方式、执行命令、关键风险和回滚思路。
- ✅ GitHub Actions 或等价自动化如果用于辅助 `sb3` 回补，只能面向“已准备好的 backport 分支”开 PR；不得在 push 时自动创建整线 `sb4 -> sb3` 同步 PR。

### 3) 主干 / Nightly 门禁

- ✅ 主干或定时流水线必须补跑较慢但高价值的验证：真实数据库兼容性、迁移验证、跨模块集成、契约校验、安全扫描或依赖漏洞扫描。
- ✅ Nightly 或独立质量流水线必须覆盖 PR 不适合频繁执行的长耗时测试，并保留报告供追踪。
- ✅ 依赖内部 fork 产物的 workflow 必须在 fresh runner 上验证“可从受控仓库解析”或“可通过受控 fallback 安装后被 consumer 解析”中的至少一种路径；不得依赖开发者本地 `.m2` 或 runner 偶然缓存。
- ✅ 使用真实认证链路的 Nightly / E2E 流水线应负责校验测试身份仍然有效、权限未漂移、测试租户状态可复用。
- ✅ Nightly/full-chain 流水线中的 real-link E2E 必须提供环境准备、seed/reset、身份检查、失败日志与截图/trace 产物，保证失败可以定位是代码问题还是环境问题。
- ✅ 真实链路 E2E job 必须显式声明依赖的服务与密钥，例如数据库、Redis、OIDC、测试 client、TOTP secret；缺失时快速失败，不允许静默降级成 mock。
- ✅ 真实链路 workflow 的 secret preflight 必须与该 workflow 的真实依赖**精确对齐**：既不能漏校验必须的 secret，也不能把并未使用的 secret 设为 mandatory 导致假红。
- ✅ 失败的主干 / Nightly 门禁必须有明确责任人处理，不能长期红灯运行。
- ✅ 对需要同时启动前端、后端、数据库、OIDC 或多服务的 real-link job，服务启动步骤必须显式化并保留失败日志；不能把关键服务启动完全隐藏在黑盒 `webServer` 或等价机制后面，导致失败时缺少定位信息。
- ✅ 新增或修改 real-link workflow 时，必须验证 fresh DB / 干净环境安装路径，而不是只在已初始化数据库或开发者本地缓存环境下验证通过。
- ✅ 如果 workflow 的 readiness 窗口会受到依赖下载或首次构建影响，必须将依赖预热、构建下载、schema 初始化等前置成本移到显式步骤，避免把“未 ready”误判成应用启动失败。
- ✅ 涉及多身份、多租户、平台/租户双口径 real-link 的 workflow，必须在 job 内显式归一化环境变量（如平台身份、租户身份、bind/readonly 身份、fallback tenant code）；不能只依赖 GitHub Actions `env:` 表达式或 trigger 差异（`push` / `schedule` / `workflow_dispatch`）来“碰巧”得到正确值。
- ✅ fresh DB / CI baseline 使用 Liquibase `sqlFile` 时，路径必须采用 changelog-relative 解析并在干净库路径上验证；不能依赖本地工作目录、classpath 偶然命中或已初始化数据库兜底。
- ✅ 已在共享环境 / fresh DB 执行过的 Liquibase changeset 不得直接改写正文来修问题；必须追加新的尾部 changeset 做幂等回填或收口，并在干净库与已执行库两种路径上验证不会触发 checksum 漂移或启动失败。
- ✅ 对依赖菜单懒加载、动态路由注入、登录后壳页收敛的 real-link workflow，必须把“菜单点击进入”和“直接 deep-link / 刷新进入”视为不同风险面；至少有一层自动化覆盖 direct deep-link / refresh，不得默认二者等价。

### 4) 发布与部署

- ✅ 发布必须基于已审查、已通过门禁的提交或标签，并使用不可变制品。
- ✅ dev / test / staging / prod 环境必须隔离配置与密钥，禁止把生产配置提交到仓库。
- ✅ 数据库发布必须使用版本化迁移脚本，并提供回滚方案或兼容性迁移策略（如 expand-contract）。
- ✅ 部署后必须自动执行健康检查、关键接口 smoke、核心指标观测或日志验证。
- ✅ 生产发布必须明确记录发布批次、负责人、时间点、变更范围和回滚入口。

### 5) 失败治理与例外

- ✅ Flaky test 必须被标记、定责、限期修复；隔离只是临时措施，不是永久豁免。
- ✅ 紧急修复若简化门禁，必须保留审批记录、风险说明、回滚方案，并在事后补跑完整验证链路。
- ✅ 流水线失败信息必须可追踪：至少保留日志、失败阶段、关键命令和产物定位方式。
- ✅ 自动化测试账号、测试 client、测试租户必须最小权限化，支持轮换、失效、重建，不得与生产身份复用。
- ✅ 任何 CI / PR / 测试门禁豁免都必须留痕：至少包含豁免项、豁免原因、补跑计划、批准依据；不能只靠口头确认或聊天记录。
- ✅ 门禁豁免必须最小化，并由 reviewer 或维护者显式确认；PR 作者不能单独批准自己的豁免。
- ✅ 若门禁依赖本机环境（数据库、前端 dev server、后端 dev server、认证链路），必须明确“环境未满足”“服务启动失败”“代码验证失败”的区分；不得把三者混报成同一类失败。

---

## 应该（Should）

### 1) 质量门禁设计

- ⚠️ 受保护分支应要求绿色 CI、至少一名审查者通过、必要时启用 CODEOWNERS。
- ⚠️ PR 快速门禁建议控制在 10 到 20 分钟内，长耗时检查下沉到并行 job 或 Nightly。
- ⚠️ 流水线应按模块、语言栈、测试层级拆分，避免所有步骤串行执行。
- ⚠️ E2E job 建议按 `smoke`、`module-real-link`、`nightly-full-chain` 分层拆分，避免一个大 job 既慢又难定位。

### 2) 发布策略

- ⚠️ 生产环境优先采用灰度、蓝绿或金丝雀发布，降低一次性全量切换风险。
- ⚠️ 前端 PR 建议提供预览环境或静态预览，便于验证权限、布局和交互回归。
- ⚠️ 高风险变更建议引入发布检查清单，覆盖迁移、权限、租户、配置、监控五类风险。

### 3) 安全与合规

- ⚠️ PR 或主干流水线应纳入密钥扫描、依赖漏洞扫描、SAST；条件允许时在预发环境补 DAST。
- ⚠️ 企业环境建议维护 SBOM，并将关键制品与发布记录关联。
- ⚠️ 重要制品建议启用签名、校验和或来源证明，便于审计。
- ⚠️ 依赖测试账号的流水线建议上传失败时的身份初始化日志、测试租户信息和认证 trace，便于定位是代码问题还是环境问题。
- ⚠️ 真实链路 E2E 建议上传 playwright trace、截图、视频、seed 日志、服务日志摘要，便于复现。
- ⚠️ 对容易受 UI 壳页、菜单懒加载、首次下载时序影响的 workflow，建议把“稳定状态断言”和“临时提示断言”拆开，优先让 CI 以 durable evidence 判定通过与否。
- ⚠️ 对第三方组件运行时契约易漂移的页面（表格树形展开、受控弹层、表单 finish、路由守卫），建议在 PR 描述或 CI 产物中附上“组件契约测试 + 真实浏览器证据”的对应关系，方便 reviewer 判断不是 stub 假绿。

---

## 可以（May）

- 💡 使用矩阵构建同时覆盖多 JDK、多 Node 版本或多数据库版本。
- 💡 使用临时环境运行 E2E、契约测试或插件兼容性验证。
- 💡 自动生成发布说明、变更摘要和回滚指引。
- 💡 将监控、告警、变更单系统与发布流程联动。

---

## 例外与裁决

### 紧急发布

- 紧急安全修复可简化非关键门禁，但必须保留最小可接受验证、审批记录和发布后观测。

### 遗留系统

- 遗留模块可分阶段接入完整门禁，但新增或修改的核心链路不应继续放在“永久例外”中。

### 冲突裁决

- 平台特定规则（90+）优先于本规范。
- 安全规范（40-security）优先于本规范中的便利性取舍。
- 测试执行内容与分层要求以 `50-testing.rules.md` 为准。

---

## 示例

### ✅ 正例：企业最小可接受门禁矩阵

```text
Local:
- format
- lint
- typecheck
- unit

PR:
- build / compile
- format + lint
- unit
- frontend typecheck
- changed-scope integration
- migration smoke (if schema changed)

Main / Nightly:
- real MySQL compatibility
- full integration
- contract / smoke
- dependency + security scan

Release:
- immutable artifact promotion
- deployment health check
- post-release smoke
- observability verification
- rollback ready
```

### ❌ 反例：带红合并 + 手工修生产

```text
- PR 测试失败先合并，想着“上线后再看”
- 生产数据库手工改表，后续再补 migration
- 发布后没有 smoke，也没有回滚入口
```

